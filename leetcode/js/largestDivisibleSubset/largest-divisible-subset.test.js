jest.dontMock('./largest-divisible-subset');

describe('largest-divisible-subset', function(){
    var func = require('./largest-divisible-subset');
    it('largest-divisible-subset', function(){
        expect(func()).toEqual(true);

    });
});
jest.dontMock('./binary-tree-paths');

describe('binary-tree-paths', function(){
    var func = require('./binary-tree-paths');
    it('binary-tree-paths', function(){
        expect(func()).toEqual(true);

    });
});
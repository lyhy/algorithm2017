jest.dontMock('./binary-tree-tilt');

describe('binary-tree-tilt', function(){
    var func = require('./binary-tree-tilt');
    it('binary-tree-tilt', function(){
        expect(func()).toEqual(true);

    });
});
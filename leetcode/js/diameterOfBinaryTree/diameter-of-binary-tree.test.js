jest.dontMock('./diameter-of-binary-tree');

describe('diameter-of-binary-tree', function(){
    var func = require('./diameter-of-binary-tree');
    it('diameter-of-binary-tree', function(){
        expect(func()).toEqual(true);

    });
});